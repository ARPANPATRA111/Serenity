rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Media mutations currently flow through the authenticated media API.
    // Keep browser access closed until a dedicated signed-upload flow exists.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
